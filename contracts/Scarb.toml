[package]
name = "veilbid_auction"
version = "0.1.0"
edition = "2024_07"
description = "VeilBid sealed-bid auction contract (commit-reveal) for Starknet"

[dependencies]
starknet = ">=2.8.0"

[[target.starknet-contract]]
sierra = true
casm = true
